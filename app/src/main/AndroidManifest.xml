<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nycschools.a20220131_michealadeniyi_nycschools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.20220131MichealAdeniyiNYCSchools">
        <activity
            android:name=".ui.schoolsat.SchoolSatActivity"
            android:exported="true"
            android:theme="@style/CustomNoActionBar.Activity"
            />
        <activity
            android:name=".ui.schoolsnyc.SchoolsNYCActivity"
            android:exported="true"
            android:theme="@style/CustomNoActionBar.Activity"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>